name: Update cidr
on: 
  push:
   branches: [master]
  schedule:
    - cron: '0 0 * * *'
jobs:
  Telegram:
    runs-on: ubuntu-latest
    outputs: 
      Shadowrocket: ${{ steps.Shadowrocket.outputs.rule }}
      ClashX: ${{ steps.ClashX.outputs.rule }}
    steps:
      - name: Shadowrocket
        id: Shadowrocket
        run: |
          Telegram="# Build Of GitHub Actions\n"
          Telegram+="# Date `date '+%Y-%m-%d %H:%M:%S'`\n"
          Telegram+="DOMAIN-SUFFIX,t.me\n"
          Telegram+="DOMAIN-SUFFIX,telegram.org\n"
          Telegram+="`curl https://core.telegram.org/resources/cidr.txt | awk -F . '{if(NF==4){print "IP-CIDR,"$0}else{print "IP-CIDR6,"$0}}'`"
          echo "::set-output name=rule::$Telegram"
      - name: ClashX
        id: ClashX
        run: |
          Telegram="# Build Of GitHub Actions\n"
          Telegram+="# Date `date '+%Y-%m-%d %H:%M:%S'`\n"
          Telegram+="payload:\n"
          Telegram+="  - DOMAIN-SUFFIX,t.me\n"
          Telegram+="  - DOMAIN-SUFFIX,telegram.org\n"
          Telegram+="`curl https://core.telegram.org/resources/cidr.txt | awk -F . '{if(NF==4){print "  - IP-CIDR,"$0}else{print "  - IP-CIDR6,"$0}}'`"
          echo "::set-output name=rule::$Telegram"
  Update:
    needs: [Telegram]
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@master
      - run: |
          cd ${{ github.workspace }}
      - name: Git push
        run: |
          echo ${{ needs.Telegram.outputs.Shadowrocket }} > ./Shadowrocket/Telegram.conf
          echo ${{ needs.Telegram.outputs.ClashX }} > ./ClashX/Telegram.conf
          git config --global user.email "you@example.com"
          git config --global user.name "GitHub Actions"
          git status
          git add *
          git commit -m update
          git push
  # Update:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Check out repository code
  #       uses: actions/checkout@master
  #     - run: |
  #         cd ${{ github.workspace }}
  #     - name: Shadowrocket Telegram
  #       run: |
  #         echo "# Build Of GitHub Actions" > ./Shadowrocket/Telegram.conf
  #         echo "# Date "`date '+%Y-%m-%d %H:%M:%S'` >> ./Shadowrocket/Telegram.conf
  #         echo "DOMAIN-SUFFIX,t.me" >> ./Shadowrocket/Telegram.conf
  #         echo "DOMAIN-SUFFIX,telegram.org" >> ./Shadowrocket/Telegram.conf
  #         curl https://core.telegram.org/resources/cidr.txt | awk -F . '{if(NF==4){print "IP-CIDR,"$0}else{print "IP-CIDR6,"$0}}' >> ./Shadowrocket/Telegram.conf
          
  #     - name: ClashX Telegram
  #       run: |
  #         echo "# Build Of GitHub Actions" > ./ClashX/Telegram.conf
  #         echo "# Date "`date '+%Y-%m-%d %H:%M:%S'` >> ./ClashX/Telegram.conf
  #         echo "payload:" > ./ClashX/Telegram.conf
  #         echo "  - DOMAIN-SUFFIX,t.me" >> ./ClashX/Telegram.conf
  #         echo "  - DOMAIN-SUFFIX,telegram.org" >> ./ClashX/Telegram.conf
  #         curl https://core.telegram.org/resources/cidr.txt | awk -F . '{if(NF==4){print "  - IP-CIDR,"$0}else{print "  - IP-CIDR6,"$0}}' >> ./ClashX/Telegram.conf
  #     - name: Git push
  #       run: |
  #         git config --global user.email "you@example.com"
  #         git config --global user.name "GitHub Actions"
  #         git add *
  #         git commit -m update
  #         git push

      # - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      # - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      # - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      # - name: Check out repository code
      #   uses: actions/checkout@v3
      # - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      # - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      # - name: List files in the repository
      #   run: |
      #     ls ${{ github.workspace }}
      # - run: echo "🍏 This job's status is ${{ job.status }}."